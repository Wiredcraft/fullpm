---
git_install_from_source: true

# Vars used to build the nodejs code
pmhub_repo_url: git@github.com:Wiredcraft/pmhub.git
pmhub_repo_version: "{{ version_pmhub }}"
pmhub_repo_env:
  NODE_ENV: "{{ node_env }}"
nodejs_version: 4.4.6
